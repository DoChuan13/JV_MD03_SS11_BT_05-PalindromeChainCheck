import Palindrome.Palindrome;

import java.util.Scanner;

public class main {
    static Scanner scanner = new Scanner(System.in);

    public static void main(String[] args) {
        Palindrome.checkPalidrome(scanner);
    }
}
